# フェーズ1: 基礎構文 TODO

## 環境セットアップ
- [x] Rust（rustup）のインストール確認
- [x] `cargo --version` で確認
- [x] エディタの設定（rust-analyzer）

## 1. プロジェクトの基本
- [x] `cargo new` で新規プロジェクト作成
- [x] `cargo run` で実行
- [x] `Cargo.toml` を読む
- [x] `println!` で Hello, world!

## 2. 変数と型
- [x] `let` と `let mut` の違いを試す
- [x] シャドーイングを試す
- [x] 各基本型（整数・浮動小数点・真偽値・文字）を使う
- [x] `&str` と `String` の違いを理解する
- [x] **Pythonとの比較メモ**: Pythonの変数とRustの所有権の違いを意識する

## 3. 関数
- [ ] 引数なし関数を書く
- [ ] 引数あり戻り値ありの関数を書く
- [ ] 式と文の違いを意識する

## 4. 制御構文
- [ ] `if` を式として使う
- [ ] `loop` で値を返す
- [ ] `for` で範囲を回す
- [ ] `match` で複数パターンを処理

## 5. 標準入出力
- [ ] `stdin().read_line()` で1行読む
- [ ] `trim()` で改行を除去
- [ ] `parse::<i32>()` で数値変換

## 6. 所有権
- [x] ムーブが起きるコードを書く
- [x] `&` で借用してみる
- [x] `&mut` で可変借用してみる
- [x] 借用チェッカに怒られるコードを意図的に書いて理解する
- [x] **Pythonとの比較メモ**: PythonのGCとRustの所有権の違いを整理する

## 7. データ構造
- [ ] 構造体を定義してインスタンス化
- [ ] 列挙型を定義
- [ ] タプルを使う
- [ ] `Vec<T>` を使う
- [ ] `HashMap` を使う

## 8. メソッド構文
- [ ] `impl` ブロックでメソッドを定義
- [ ] `&self` と `&mut self` を使い分ける
- [ ] `Self::new()` で関連関数を作る

## 9. パターンマッチ
- [ ] `match` で網羅的にパターン処理
- [ ] `if let` を使う

## 10. エラー処理
- [ ] `Option<T>` を返す関数を書く
- [ ] `Result<T, E>` を返す関数を書く
- [ ] `?` 演算子でエラーを伝播
- [ ] `panic!` を試す
- [ ] **Pythonとの比較メモ**: Pythonの例外とRustの`Result`型の違いを整理する

## 11. モジュール
- [ ] ファイルを分割してモジュール化
- [ ] `use` でインポート

## 12. トレイト
- [ ] `#[derive(Debug)]` で `{:?}` 出力
- [ ] 自前のトレイトを定義して実装

## 13. ジェネリクス
- [ ] ジェネリック関数を書く
- [ ] ジェネリック構造体を書く

## 14. イテレータ
- [ ] `Vec` を `iter().map().collect()` で変換
- [ ] `filter` で絞り込み

## 15. テスト基礎
- [ ] `#[test]` を付けたテスト関数を書く
- [ ] `cargo test` で実行
- [ ] `assert_eq!` / `assert!` を使う

## 練習問題
- [ ] FizzBuzz
- [ ] 単語カウンター（`&str` を引数に取る版と `String` を所有させる版の両方を書く）
- [ ] 簡易電卓（標準入力から数式を読み取って計算）

## フェーズ完了チェック
- [ ] 全TODOにチェックが入った
- [ ] フェーズ2に進む準備完了
